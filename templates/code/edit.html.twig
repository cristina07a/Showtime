{% extends 'base.html.twig' %}

{% block title %}Edit Code{% endblock %}

{% block content %}
    <h1 class="text-xl italic font-bold text-center pt-4">Edit code with index {{ code.id }}</h1>

    {{ include('code/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="flex gap-2 mt-4 justify-center">
        <button class="btn btn-soft"
                onclick="window.location.href='{{ path('app_code_index') }}'"> Back to list
        </button>
        {{ include('code/_delete_form.html.twig') }}
    </div>
{% endblock %}
