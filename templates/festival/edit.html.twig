{% extends 'base.html.twig' %}

{% block title %}Edit Festival{% endblock %}

{% block content %}
    <h1 class="text-xl italic font-bold text-center pt-4">Edit festival with index {{ festival.id }}</h1>

    {{ include('festival/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="flex gap-2 mt-4 justify-center">
        <button class="btn btn-soft"
                onclick="window.location.href='{{ path('app_festival_index') }}'"> Back to list
        </button>
        {{ include('festival/_delete_form.html.twig') }}
    </div>
{% endblock %}
