{% extends 'base.html.twig' %}

{% block title %}New Festival{% endblock %}

{% block content %}
    <h1 class="text-xl italic font-bold text-center pt-4">Create new festival</h1>

    {{ include('festival/_form.html.twig') }}

    <div class="flex gap-2 mt-4 justify-center">
        <button class="btn btn-soft"
                onclick="window.location.href='{{ path('app_festival_index') }}'"> Back to list
        </button>
    </div>
{% endblock %}
